
import React from 'react';
import { Calendar, X } from 'lucide-react';

const CreateEventModal: React.FC<{ onSave: any, onClose: () => void }> = ({ onClose }) => {
  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50">
        <div className="bg-white p-6 rounded-xl w-96">
            <div className="flex justify-between mb-4">
                <h3 className="font-bold flex gap-2"><Calendar/> New Event</h3>
                <button onClick={onClose}><X/></button>
            </div>
            <button onClick={onClose} className="mt-4 w-full bg-indigo-600 text-white py-2 rounded font-bold">Create</button>
        </div>
    </div>
  );
};

export default CreateEventModal;
